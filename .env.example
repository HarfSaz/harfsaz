# Copy to .env (or export in your shell) before running `pnpm app:dev`.
# Qalam reads the Claude API key from the environment — it is never bundled or
# sent to the frontend.
QALAM_ANTHROPIC_API_KEY=sk-ant-...
