# Copy to .env (or export in your shell) before running `pnpm app:dev`.
# Harfsaz reads the Claude API key from the environment — it is never bundled or
# sent to the frontend.
HARFSAZ_ANTHROPIC_API_KEY=sk-ant-...
