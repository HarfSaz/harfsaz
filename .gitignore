# dependencies
node_modules
.pnpm-store

# build output
dist
src-tauri/target
src-tauri/gen

# env / secrets
.env
.env.local

# editor / os
.DS_Store
*.log
.vscode
.idea

# claude code local settings
.claude/settings.local.json

# Local QA captures and private deployment configuration
.playwright-mcp/
.env.*
!.env.example
docs/releases/production-dns.md

# Tests are intentionally NOT published in the open-source editor repository.
# Some of them import server-side sources from the private harfsaz-cloud repo,
# so publishing them would leak private billing/service internals. They live in
# harfsaz-cloud/tests-editor/ instead. (A .git/info/exclude entry was used once
# and did not survive, because it is local-only and never committed.)
tests/
